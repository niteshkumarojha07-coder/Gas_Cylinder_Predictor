name: gas_cylinder_predictor

description: A new Flutter project for predicting gas cylinder needs.
	environment:
      sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.0.0
  provider: ^6.0.0

flutter:
  uses-material-design: true
